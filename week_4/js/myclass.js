// JavaScript Document
/*
 * Figure this file out on your own!!!!
 */
var MyClass = new Class({
	
	Implements: [Options, Events],
	
	options:{},

	initialize: function(element, options)
	{
		this.element = document.id(element);
		this.setOptions(options);
		/*
		 * Tasks:
		 * Select the input text box, and save it in a variable
		 * Select the button, and save it in a variable 
		 * Make the button clickable to a function you write
		 */
	}
	
	/*
	 * Create a function that uses the phrase in the options object
	 * (oh, yeah, set that up too) and outputs it to the input box
	 * you selected in the initialize function.
	 */
});